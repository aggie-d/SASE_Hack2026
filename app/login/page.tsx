import { PageShell } from "@/components/PageShell";

export default function LoginPage() {
  return (
    <PageShell
      title="Sign in"
      description="Sign up or enter a seeded demo account."
    />
  );
}
